name: Deploy to Vercel (Production)

on:
  push:
    branches: [main]
  workflow_dispatch:

# Org/Project IDs are identifiers, not secrets. Only VERCEL_TOKEN is sensitive.
env:
  VERCEL_ORG_ID: team_GkxbST88Jc0dWvj9nnEWHvxx
  VERCEL_PROJECT_ID: prj_pKnUQEKeeFfxL0YySFDgjVWRer6u

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install Vercel CLI
        run: npm install -g vercel@latest
      - name: Pull Vercel project settings
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
      - name: Build
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
      - name: Deploy to production
        run: |
          url=$(vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }} | tail -n1)
          echo "Deployed: $url"
          # Re-point the clean production alias at this deployment.
          vercel alias set "$url" 2d-web-quest.vercel.app --token=${{ secrets.VERCEL_TOKEN }} --scope=meta-vr
